<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Liffchaser</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    
    <header>
      
      <a href="/"><h1>Liffchaser</h1></a>
      
      <nav>
        <!-- Conditionally render login or logout links -->
        <ul class="nav justify-content-end">
          <li class="nav-item"><a id="news" class="nav-link" aria-current="page" href="/news">News</a></li>
           <li class="nav-items"><a id="aboutus" class="nav-link" aria-current="page" href="/aboutus">About Us</a></li>
        {{#if logged_in}}
          <li class="nav-item"><a id="profile" class="nav-link" aria-current="page" href="/profile">Home</a></li>
          <li class="nav-item"><a id="logout" class="nav-link" aria-current="page">Log Out</a></li>

          {{else}}
          <li class="nav-item"><a href="/login" class="nav-link" aria-current="page">login</a></li>
        {{/if}}
        </ul>
      </nav>
            
    </header>

    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>


</html>
